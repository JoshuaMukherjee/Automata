package automata;


/**
 * regex = char | regex"|"regex | regexregex | regex* 
 * char = {a-z}|{A-Z}|null
 */
public class Parser{
    /**
     * Returns {@code int} based on highest level type
     * 
     * 0 if not valid syntax
     * 
     * @param regex
     * @return type of regex
     */
    public static int getType(String regex){
        return 0;
        
    }
}